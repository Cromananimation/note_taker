{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Heroku Push",
            "type": "shell",
            "command": "heroku login && git push heroku main && git push origin main"
        }
    ]
}